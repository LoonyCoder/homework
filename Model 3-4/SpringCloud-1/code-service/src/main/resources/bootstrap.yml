server:
  port: 8081

spring:
  application:
    name: code-server
  cloud:
    config:
      name: common-config #配置⽂件名称
      profile: dev #后缀名称
      label: master #分⽀名称
      uri: http://localhost:9006

ribbon:
  ReadTimeout: 10000
  ConnectTimeout: 10000



